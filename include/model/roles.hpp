#include <gtkmm.h>

namespace model
{
    class roles
    {
    private:
        /* data */
    public:
        roles(/* args */);
        ~roles();
    };

    roles::roles(/* args */)
    {
    }

    roles::~roles()
    {
    }

} // namespace model
